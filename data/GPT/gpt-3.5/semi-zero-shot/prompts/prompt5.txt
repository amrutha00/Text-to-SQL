With the TPC-DS dataset as a reference, using the table and field names from the TPC-DS benchmark, convert natural language directive provided as input into an SQL query in the Netezza dialect?
Input: Reproduce a report that provides the sales, profit, return amount, and net loss for the store, catalog, and web channels within a 14-day time frame starting from '2000-08-19'. The results are grouped by sales channel and specific sales method (store for store sales, catalog page for catalog sales, and web site for web sales). The report is limited to the top 100 results and ordered by channel and specific method.